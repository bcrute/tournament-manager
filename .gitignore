node_modules/
dist/
__pycache__/
*.pyc
.venv/
.env
# server secrets live only on the deployment, never in the repo
mtg.env

# test artifacts
.coverage
coverage/
frontend/test-results/
frontend/playwright-report/

# stray db from running the app inside backend/
backend/treachery.db

# SQLite sidecars from a local run — transient, never the database itself
*.db-shm
*.db-wal
